---
description: Run full project validation (Lint, Test, Build) using Turbo
---

# Validation Workflow

This workflow runs the TurboRepo pipeline to ensure code quality and build integrity.

// turbo

1. Run the check command

   ```bash
   pnpm check
   ```

2. (Optional) Run tests specifically
   ```bash
   pnpm test
   ```
